# Layer 04d: Environmental ROI Budget Optimizer

This sub-layer ranks repair tasks by environmental return per rupee. It uses a 0/1 knapsack and greedy approximation algorithms to optimize the budget.
